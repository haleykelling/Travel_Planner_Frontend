import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const Trip = () => {
    return (
        <View>
            <Text>This is a Trip Component</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    
})

export default Trip;
