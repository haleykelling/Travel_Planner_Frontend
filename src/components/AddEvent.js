import React from 'react';
import {TouchableOpacity, View, Text, StyleSheet} from 'react-native';

const AddEvent = () => {
    return (
        <TouchableOpacity>
            <Text>Click here to add an event</Text>
        </TouchableOpacity>
    );
}

export default AddEvent;
